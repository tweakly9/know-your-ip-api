{
  "name": "know-your-ip-api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "update-deps": "npm i -g npm-check-updates && ncu -u && npm install --legacy-peer-deps"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.15",
    "vitest": "3.0.5",
    "wrangler": "^3.109.0"
  },
  "dependencies": {
    "hono": "^4.7.1"
  }
}
